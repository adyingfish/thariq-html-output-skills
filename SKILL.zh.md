---
name: thariq-html-output
description: >-
  当请求的对象具有某种会被线性文本压平的「形状」时，产出单个自包含的 HTML 产物，
  而非 Markdown 文档：需要权衡的比较、空间信息（差异、调用图、模块地图、流程图、
  时间轴）、必须亲身感受的动效或交互、设计界面、需要非线性浏览的参考文档、幻灯片、
  讲解、周期性报告，或为某个难以在文本框中描述的任务而生的一次性编辑器。判断标准就是
  Thariq Shihipar 的那句话：读者会「扫一眼」Markdown 版，但会「真正阅读」HTML 版吗？
  当用户要求计划、比较、代码审查、PR 说明、模块讲解、设计系统、组件示例表、原型、动效、
  图表、流程图、幻灯片、讲解、概念梳理、状态更新、故障复盘、事故时间轴，或一次性编辑器/
  工具时——即便用户从未说「HTML」或「产物」——也应触发。仅在以下情况保持 Markdown：
  简短的对话式回复、纯代码回答、终端/命令回答，以及真正只有几句话的内容。
---

# HTML output

> "Twenty self-contained `.html` files an agent produced instead of a wall of
> markdown. Each one trades a document you'd skim for one you'd actually read."
> — Thariq Shihipar, *The Unreasonable Effectiveness of HTML*
>
> 「二十个自包含的 `.html` 文件，由智能体产出，取代了一面 Markdown 文字墙。每一个都把
> 一份你只会略读的文档，换成了一份你会真正阅读的文档。」
> —— Thariq Shihipar，《HTML 的惊人有效性》

本技能是对 Thariq 这篇文章的忠实操作化。它原封不动地保留了他的九个类别，用他的措辞，
也保留了他那份轻巧的分寸感。原文刻意给的是例子而非规则，因为——用他自己的话说——
「你并不需要做太多，就能让 agent 实现这一点」。本技能力图成为保留这一点的最小脚手架，
而非一座写满清规戒律的堡垒。

**哪些来自哪里。** 九个类别及其 demo 描述、「略读 vs 真正阅读」的判断、单文件自包含、「编辑器永远以导出收尾」，
这些观察出自 Thariq 的文章；示例画廊——20 个主例加「Know your unknowns」的 11 个——也是他的，原样快照在
`references/original-examples/`。把这些观察提炼成识别测试、组合模型与三项义务，以及读者动作表、隐式触发规则、
自检、各参考文件的「直接入口」表、把 Unknowns 画廊纳为跨类别参考，都是技能层的增补。完整的归属表与 31 例索引见
`references/zh/source-and-examples.zh.md`。

## 识别测试

在一切之前，先问那个贯穿全文的核心问题：

> **我即将写的东西，是否具有一个会被线性文本压平的形状——读者会「扫一眼」它的 Markdown 版，
> 但会「真正阅读」它的 HTML 版吗？**

若是，就做一个 HTML 产物。若答案确实为否——它只是一句话、一段代码、一条命令——那就保持
Markdown。这条豁免是真实存在且重要的，见下文「何时保持 Markdown」。一个把*每条*提示词
都转成 HTML 的技能，恰恰是 Thariq 所担忧的那个东西。

## 九面镜头

这是 Thariq 的九个类别，未作改动。把它们当作**镜头，而非分桶**。一个请求很少孤立地是
「一张图」或「一份计划」——它有一个或多个形状，每个形状都是你构建时透过去观察的一面镜头。

| 镜头 | 在请求关于以下内容时透过它观察…… | 参考文件 |
|---|---|---|
| 探索与规划 | 并排权衡选项，或把决定转化为可交接的计划 | `references/zh/exploration-and-planning.zh.md` |
| 代码审查与理解 | 注释式差异、PR 说明、模块地图、「解释这个包」 | `references/zh/code-review-and-understanding.zh.md` |
| 设计 | 设计系统、令牌、组件接触表 | `references/zh/design.zh.md` |
| 原型 | 动效、过渡、可点击流程——必须亲身感受的东西 | `references/zh/prototyping.zh.md` |
| 插图与图表 | 内联 SVG 图形、流程图、架构图 | `references/zh/illustrations-and-diagrams.zh.md` |
| 幻灯片 | 一份可在会议上用方向键翻阅的简短演示 | `references/zh/decks.zh.md` |
| 研究与学习 | 讲解、概念梳理、为新主题搭建可浏览的脚手架 | `references/zh/research-and-learning.zh.md` |
| 报告 | 状态更新、故障复盘、事故时间轴 | `references/zh/reports.zh.md` |
| 自定义编辑界面 | 为难以打字描述的任务而生的一次性编辑器 | `references/zh/custom-editing-interfaces.zh.md` |

## 让读者的动作决定产物的形态

在命名镜头之前，先辨认读者必须对这份产物*做什么*：

| 读者动作 | 主导形态 |
|---|---|
| **比较** — 并排权衡选项 | 目光横向扫过的列或比较表 |
| **定位** — 在众多信息中找到一条 | 以维度为入口的可筛选/可检索索引 |
| **追踪** — 沿路径或事件序列前进 | 带标签节点、有方向边或时间轴的流向 |
| **试验** — 与机制互动来理解它 | 改变参数能产生可见后果的实时控件 |
| **调整** — 编辑配置并带走结果 | 带实时预览和能捕获状态的导出的可编辑输入 |
| **跟随** — 以固定节奏吸收一段叙述 | 有意节奏、每个节拍一个想法的幻灯片或分页故事 |

选择把那个动作放在最显眼位置的形态。**自检：** 读者要完成的动作——比较、定位、改变、试验——是否是页面上首先可见的东西？如果首屏显示的是标题和段落而非那个动作本身，检查是选错了主导形态，还是文档布局确实是这次材料最直接的表达。

**隐式触发规则。** 有些请求不命名任何形态但暗示了一种："哪个更好/更快/更安全" → 比较；"解释这段代码怎么运行"或"追踪……时发生了什么" → 追踪或定位；"我还没决定" → 试验或调整；"带我走一遍" → 跟随。

## 组合是默认，而非例外

这是本技能的核心，它直接生长自 Thariq 自己的*实施计划* demo，他这样描述它：

> "Milestones on a timeline, a data-flow diagram, inline mockups, the risky
> code, and a risk table — the plan you hand off."
>
> 「时间轴上的里程碑、一张数据流图、内联原型、关键代码，以及一张风险表——你交接出去的计划。」

那一个产物**同时是探索 + 插图 + 设计 + 代码审查**。镜头是会叠加的。所以：

1. **先辨认主导读者动作**（见上表），再命名支持它的镜头。
2. **读取每一个被点名的参考文件。** 它们都很简短，而且是为叠加而写的。查看该镜头最接近的原作示例——借鉴它的空间关系、信息层级和读者动作，然后按本次材料重新设计，而非复制它的外壳。
3. **让主导镜头确定骨架**，让其余镜头在骨架内贡献各自的模式。上面那份实施计划，是「探索」的骨架（问题 → 选项落定 → 交接），里面嵌入了来自「插图」的一张图、来自「设计」的原型，以及来自「代码审查」的一段注释式片段。

在任务有帮助的地方组合辅助镜头，不帮助时就停。镜头更多并不等于产物更丰富——累积了所有镜头的产物，会退化成一份通用报告。

若仍有影响任务的偏好、假设或知识缺口，按需要查阅 `references/zh/unknowns.zh.md`，用探索、原型、澄清或理解检查来帮助用户判断，并带走有用的决策和未决问题；使用已给出的答案，避免不必要的关卡。

## 每个产物欠读者的

无论由哪些镜头构建，每个产物都应守住下面这些。它们不是一份风格指南——每一条都是 Thariq 所说的
那个分水岭的一部分：「你只会略读的页面」与「你会真正阅读的页面」之间的差别。归结为三项义务。

**它必须能独立站住。** 一个 `.html` 文件，无需构建步骤、无需安装就能在浏览器打开——样式、脚本、
图像（内联 SVG 或 data URI）全在这一个文件里——拔掉网线也照样能用。若字体或库不得不来自 CDN，
挑一个稳定的，并假设它日后可能需要内联。在手机上它应当保持形状而非塌掉：带上 viewport 标签，
以及一个能在窄列里存活的布局。

**它必须显出自己的形状。** 这正是你绕开 Markdown 的全部理由，所以力气要花在这里。比较就铺成列；
时间轴就画成时间轴；差异就渲染成差异。把一摞 Markdown 标题 1:1 抄成 `<h2>`，等于把要点扔掉。
而且它要在大约五秒内落地——一个标题、一句定位的话、然后是实质——好让读者在决定读它之前，就知道
自己手里拿的是什么。

**它必须保持诚实。** 克制是默认：可读的字体、舒适的行宽、足够的呼吸空间，色彩只用在它有意义的
地方。一旦产物让读者改变状态——拖动、切换、调参——它就欠读者一条出路：一个「复制为 Markdown /
复制为 JSON / 下载」的控件，把被操作过的界面变回文本。没有这条出路，编辑器就是死胡同；有了它，
用 Thariq 的话说，闭环会收得更紧。

## 何时答案就是 Markdown

识别测试是双向的。当内容没有可压平的形状时，HTML 只是繁文缛节——而硬要伸手去拿，恰恰是 Thariq
警惕的那种机械反射。当回复是你会在对话里随口说出的那类东西时，就留在 Markdown：一两句话的回答、
一段别人会原样粘走的代码、一小串 shell 命令、一个扫一眼就丢的清单。这些都不会因为多了一列、一种
颜色或一次点击而变好。

有一种情况更微妙——一份长期存在于版本控制、会在多轮 PR 中被审查的文档。HTML 差异噪音大，而
Markdown 差异干净，所以这份长寿的规格文档作为 Markdown 更友好，哪怕今天为它单独生成一个 HTML
*视图*会更好读。

## 文件去哪

把产物作为单个 `.html` 写到工作目录，名字要说清它是什么（`pagination-options.html`、
`deploy-pipeline.html`），然后把路径指给用户，并提议替他打开。当一个请求催生出一组本就属于一起的
文件——比如一次探索长成一份计划——把它们放进同一个文件夹，让它们作为整体一起流转。一切都自包含进这
一个文件：内联样式与脚本，编辑器状态放在内存里，并优先用一份光秃秃的 `text/html` 文档，而非 React、
Mermaid 或一张裸 SVG，除非请求明确要的就是其中之一。

**交接给更小的模型或子智能体。** 各参考文件被写成可独立成立的，所以一个更便宜的执行者只需三样
东西就能冷启动构建产物：原始请求、上面那三项义务，以及每个被点名镜头的参考文件全文。别的什么都
不必带上。

## 关于克制

丑陋的产物比朴素的 Markdown 更糟，而此处的丑有一个可预料的来源：那套生成式 UI 的「家居风」——
每个面块都是一张带色卡片，每个标题都顶着一个 emoji，一排几乎雷同的强调色却毫无用处。解法不是在
上面再叠一层品味，而是减法。从安静的排版起步（文档用真正的阅读字体，工具用中性的无衬线），给它
留白，只在某种颜色真正有活儿可干的那一刻才引入它——一个状态、一个严重程度、一个坐标轴。如果项目
已有设计系统或 `frontend-design` 技能，取它的令牌，而非自己发明。标杆是 Thariq 的：他的例子看起来
像是有人坐下来读过的东西，而非机器吐出来的东西。朝那里去。

## 关于本技能不是什么

它不是「总是用 HTML 回答」。重点从来不是格式。重点在于：对许多人们如今要求智能体产出的东西
——计划、比较、审查、讲解、编辑器——Markdown 那种线性、无色、静止的形状会*主动遮蔽内容*，而
HTML 解除了这层束缚。在 Markdown 是更好媒介的地方，就用 Markdown。本技能是识别，而非反射。
